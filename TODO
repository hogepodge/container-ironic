* wait for it needs to check real endpoints
* get neutron network and set CLEANING_NETWORK and PROVISIONING_NETWORK
* set swift glance account `openstack object store account show`
* create tempkey `openstack object store account set --property Temp-Url-Key=${SWIFT_TEMP_URL_KEY}`
* document ip6tables-restore: unable to initialize table 'filter' (need to load ip6_tables kernel module)
* document ebtables kernel module loading
